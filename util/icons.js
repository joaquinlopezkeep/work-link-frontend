import { Icon, Icons } from '@ui-kitten/components';

export const EmailIcon = props => {
	return <Icon {...props} name='email-outline' />;
};
export const EyeIcon = props => {
	return <Icon {...props} name='eye-off' />;
};
